class CreateTable<%= image_class.classify.pluralize %> < ActiveRecord::Migration
  def change
    create_table(:mosaico_rails_<%= image_class.pluralize.underscore %>) do |t|
      t.integer  "<%= "#{image_class}_id" %>"
      t.integer  "<%= "mosaico_rails_#{gallery_class}_id" %>"
      t.datetime "created_at",         null: false
      t.datetime "updated_at",         null: false
      t.string   "image_file_name"
      t.string   "image_content_type"
      t.integer  "image_file_size"
      t.datetime "image_updated_at"
      t.string "image_url"
      t.index ["<%= "#{image_class}_id" %>"], name: "index_<%= "#{image_class}".pluralize %>_on_<%= "#{gallery_class}_id" %>, using: :btree"
    end
  end
end
